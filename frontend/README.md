# FIELDWORK — Frontend

Vite + React + TypeScript frontend for FIELDWORK. See the [root README](../README.md) for full setup (this app needs the backend running).

```bash
cp .env.example .env   # set VITE_API_URL
npm install
npm run dev             # http://localhost:5173
```
