# Local development — points at the Express backend running via `npm run dev`.
VITE_API_URL=http://localhost:3000/api

# Production — set this as an Environment Variable in the Vercel project
# settings (not in a committed .env file), pointing at your deployed backend:
# VITE_API_URL=https://fieldwork-api.vercel.app/api
